const crypto = require('crypto');
const prisma = require('../config/database');
const { encrypt } = require('../services/encryption.service');
const { WRITE_ROLES, ADMIN_ROLES, requireWorkspaceRole, projectAccess, collectionAccess } = require('../services/authorization.service');

const fail = (message, statusCode = 400) => Object.assign(new Error(message), { statusCode });
const clean = (value) => typeof value === 'string' ? value.trim() : value;
const slug = (value) => clean(value).toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/(^-|-$)/g, '');
const page = (res, data, status = 200) => res.status(status).json({ success: true, data });
const checkName = (name) => { if (!clean(name)) throw fail('Name is required'); };
async function activity(workspaceId, userId, type, entityType, entityId, metadata = {}) { return prisma.activity.create({ data: { workspaceId, userId, type, entityType, entityId, metadata } }); }

async function listWorkspaces(req, res) { page(res, await prisma.workspace.findMany({ where: { members: { some: { userId: req.user.id, status: 'ACTIVE' } } }, include: { _count: { select: { projects: true, members: true } } }, orderBy: { updatedAt: 'desc' } })); }
async function createWorkspace(req, res) {
  checkName(req.body.name); const workspace = await prisma.workspace.create({ data: { name: clean(req.body.name), slug: slug(req.body.slug || req.body.name), description: clean(req.body.description) || null, ownerId: req.user.id, members: { create: { userId: req.user.id, role: 'OWNER' } }, projects: { create: { name: 'Default Project', description: 'Proyecto inicial del workspace' } } } });
  page(res, workspace, 201);
}
async function getWorkspace(req, res) { await requireWorkspaceRole(req.user.id, req.params.workspaceId); page(res, await prisma.workspace.findUnique({ where: { id: req.params.workspaceId }, include: { members: { include: { user: { select: { id: true, name: true, email: true, avatarUrl: true } } } }, projects: true } })); }
async function updateWorkspace(req, res) { await requireWorkspaceRole(req.user.id, req.params.workspaceId, ADMIN_ROLES); const inviteCode = req.body.regenerateInviteCode ? `WS-${crypto.randomBytes(4).toString('hex').toUpperCase()}-${crypto.randomBytes(2).toString('hex').toUpperCase()}` : undefined; page(res, await prisma.workspace.update({ where: { id: req.params.workspaceId }, data: { ...(req.body.name && { name: clean(req.body.name) }), ...(req.body.description !== undefined && { description: clean(req.body.description) }), ...(inviteCode && { inviteCode }) } })); }
async function removeWorkspace(req, res) { const member = await requireWorkspaceRole(req.user.id, req.params.workspaceId, new Set(['OWNER'])); await prisma.workspace.delete({ where: { id: member.workspaceId } }); res.status(204).end(); }

async function listProjects(req, res) { await requireWorkspaceRole(req.user.id, req.params.workspaceId); page(res, await prisma.project.findMany({ where: { workspaceId: req.params.workspaceId }, include: { _count: { select: { collections: true, apiKeys: true } } }, orderBy: { updatedAt: 'desc' } })); }
async function createProject(req, res) { await requireWorkspaceRole(req.user.id, req.params.workspaceId, WRITE_ROLES); checkName(req.body.name); const project = await prisma.project.create({ data: { workspaceId: req.params.workspaceId, name: clean(req.body.name), description: clean(req.body.description) || null, environments: { create: [{ name: 'Producción', slug: 'production', isDefault: true }, { name: 'Staging / Dev', slug: 'staging-dev' }] } } }); await activity(req.params.workspaceId, req.user.id, 'PROJECT_CREATED', 'project', project.id); page(res, project, 201); }
async function getProject(req, res) { const project = await projectAccess(req.user.id, req.params.projectId); page(res, await prisma.project.findUnique({ where: { id: project.id }, include: { collections: { include: { requests: true }, orderBy: { sortOrder: 'asc' } }, environments: true, apiKeys: { where: { status: { not: 'REVOKED' } }, select: { id: true, name: true, prefix: true, lastFourCharacters: true, scopes: true, status: true, createdAt: true, lastUsedAt: true } } } })); }
async function updateProject(req, res) { const project = await projectAccess(req.user.id, req.params.projectId, WRITE_ROLES); page(res, await prisma.project.update({ where: { id: project.id }, data: { ...(req.body.name && { name: clean(req.body.name) }), ...(req.body.description !== undefined && { description: clean(req.body.description) || null }), ...(req.body.status && { status: req.body.status }) } })); }
async function removeProject(req, res) { const project = await projectAccess(req.user.id, req.params.projectId, ADMIN_ROLES); await prisma.project.delete({ where: { id: project.id } }); res.status(204).end(); }

async function listCollections(req, res) { await projectAccess(req.user.id, req.params.projectId); page(res, await prisma.collection.findMany({ where: { projectId: req.params.projectId }, include: { requests: true }, orderBy: { sortOrder: 'asc' } })); }
async function createCollection(req, res) { const project = await projectAccess(req.user.id, req.params.projectId, WRITE_ROLES); checkName(req.body.name); const parentId = clean(req.body.parentId) || null; if (parentId) { const parent = await prisma.collection.findFirst({ where: { id: parentId, projectId: project.id } }); if (!parent) throw fail('Parent collection not found', 404); } const collection = await prisma.collection.create({ data: { projectId: project.id, parentId, name: clean(req.body.name), description: clean(req.body.description) || null, sortOrder: Number.isInteger(req.body.sortOrder) ? req.body.sortOrder : 0 } }); await activity(project.workspaceId, req.user.id, 'COLLECTION_CREATED', 'collection', collection.id); page(res, collection, 201); }
async function updateCollection(req, res) { const collection = await collectionAccess(req.user.id, req.params.collectionId, WRITE_ROLES); const parentId = req.body.parentId === undefined ? undefined : (clean(req.body.parentId) || null); if (parentId) { if (parentId === collection.id) throw fail('A collection cannot be its own parent'); const parent = await prisma.collection.findFirst({ where: { id: parentId, projectId: collection.projectId } }); if (!parent) throw fail('Parent collection not found', 404); } page(res, await prisma.collection.update({ where: { id: collection.id }, data: { ...(req.body.name && { name: clean(req.body.name) }), ...(req.body.description !== undefined && { description: clean(req.body.description) || null }), ...(parentId !== undefined && { parentId }), ...(req.body.authorization && { authorization: req.body.authorization }), ...(typeof req.body.preRequestScript === 'string' && { preRequestScript: req.body.preRequestScript }), ...(typeof req.body.testScript === 'string' && { testScript: req.body.testScript }), ...(Number.isInteger(req.body.sortOrder) && { sortOrder: req.body.sortOrder }) } })); }
async function removeCollection(req, res) { const collection = await collectionAccess(req.user.id, req.params.collectionId, WRITE_ROLES); await prisma.collection.delete({ where: { id: collection.id } }); res.status(204).end(); }

function requestData(body) { checkName(body.name); if (!['GET','POST','PUT','PATCH','DELETE','HEAD','OPTIONS'].includes(body.method)) throw fail('Invalid HTTP method'); if (!clean(body.path)?.startsWith('/')) throw fail('Path must begin with /'); return { name: clean(body.name), method: body.method, path: clean(body.path), url: clean(body.url) || null, description: clean(body.description) || null, headers: Array.isArray(body.headers) ? body.headers : [], params: Array.isArray(body.params) ? body.params : [], body: typeof body.body === 'string' ? body.body : null, responseSample: typeof body.responseSample === 'string' ? body.responseSample : null, authorization: body.authorization && typeof body.authorization === 'object' ? body.authorization : {}, preRequestScript: typeof body.preRequestScript === 'string' ? body.preRequestScript : null, testScript: typeof body.testScript === 'string' ? body.testScript : null }; }
async function listRequests(req, res) { await collectionAccess(req.user.id, req.params.collectionId); page(res, await prisma.apiRequest.findMany({ where: { collectionId: req.params.collectionId }, orderBy: { updatedAt: 'desc' } })); }
async function createRequest(req, res) { const collection = await collectionAccess(req.user.id, req.params.collectionId, WRITE_ROLES); const item = await prisma.apiRequest.create({ data: { collectionId: collection.id, ...requestData(req.body) } }); await activity(collection.project.workspaceId, req.user.id, 'REQUEST_CREATED', 'request', item.id); page(res, item, 201); }
async function updateRequest(req, res) { const item = await prisma.apiRequest.findUnique({ where: { id: req.params.requestId }, include: { collection: { include: { project: true } } } }); if (!item) throw fail('Request not found', 404); await requireWorkspaceRole(req.user.id, item.collection.project.workspaceId, WRITE_ROLES); page(res, await prisma.apiRequest.update({ where: { id: item.id }, data: requestData({ ...item, ...req.body }) })); }
async function removeRequest(req, res) { const item = await prisma.apiRequest.findUnique({ where: { id: req.params.requestId }, include: { collection: { include: { project: true } } } }); if (!item) throw fail('Request not found', 404); await requireWorkspaceRole(req.user.id, item.collection.project.workspaceId, WRITE_ROLES); await prisma.apiRequest.delete({ where: { id: item.id } }); res.status(204).end(); }

async function listEnvironments(req, res) { await projectAccess(req.user.id, req.params.projectId); page(res, await prisma.environment.findMany({ where: { projectId: req.params.projectId } })); }
async function createEnvironment(req, res) { const project = await projectAccess(req.user.id, req.params.projectId, WRITE_ROLES); checkName(req.body.name); const environment = await prisma.environment.create({ data: { projectId: project.id, name: clean(req.body.name), slug: slug(req.body.slug || req.body.name), baseUrl: clean(req.body.baseUrl) || null, isDefault: !!req.body.isDefault } }); await activity(project.workspaceId, req.user.id, 'ENVIRONMENT_CREATED', 'environment', environment.id); page(res, environment, 201); }
async function listSecrets(req, res) { const env = await prisma.environment.findUnique({ where: { id: req.params.environmentId }, include: { project: true } }); if (!env) throw fail('Environment not found', 404); await requireWorkspaceRole(req.user.id, env.project.workspaceId); page(res, await prisma.secret.findMany({ where: { environmentId: env.id }, select: { id: true, name: true, encryptionKeyVersion: true, createdAt: true, updatedAt: true } })); }
async function createSecret(req, res) { const env = await prisma.environment.findUnique({ where: { id: req.params.environmentId }, include: { project: true } }); if (!env) throw fail('Environment not found', 404); await requireWorkspaceRole(req.user.id, env.project.workspaceId, WRITE_ROLES); checkName(req.body.name); if (typeof req.body.value !== 'string' || !req.body.value) throw fail('Secret value is required'); const secret = await prisma.secret.upsert({ where: { environmentId_name: { environmentId: env.id, name: clean(req.body.name) } }, update: { encryptedValue: encrypt(req.body.value) }, create: { environmentId: env.id, name: clean(req.body.name), encryptedValue: encrypt(req.body.value) } }); await activity(env.project.workspaceId, req.user.id, 'SECRET_CREATED', 'secret', secret.id); page(res, { id: secret.id, name: secret.name, createdAt: secret.createdAt }, 201); }
async function removeSecret(req, res) { const secret = await prisma.secret.findUnique({ where: { id: req.params.secretId }, include: { environment: { include: { project: true } } } }); if (!secret) throw fail('Secret not found', 404); await requireWorkspaceRole(req.user.id, secret.environment.project.workspaceId, WRITE_ROLES); await prisma.secret.delete({ where: { id: secret.id } }); res.status(204).end(); }

async function listApiKeys(req, res) { await projectAccess(req.user.id, req.params.projectId); page(res, await prisma.apiKey.findMany({ where: { projectId: req.params.projectId, status: { not: 'REVOKED' } }, select: { id: true, name: true, prefix: true, lastFourCharacters: true, scopes: true, status: true, expiresAt: true, lastUsedAt: true, revokedAt: true, createdAt: true }, orderBy: { createdAt: 'desc' } })); }
async function createApiKey(req, res) { const project = await projectAccess(req.user.id, req.params.projectId, WRITE_ROLES); checkName(req.body.name); if (typeof req.body.key !== 'string' || !req.body.key.trim()) throw fail('API key value is required'); const prefix = req.body.prefix === 'sk_test' ? 'sk_test' : 'sk_live'; const raw = req.body.key.trim(); const key = await prisma.apiKey.create({ data: { projectId: project.id, name: clean(req.body.name), prefix, keyHash: crypto.createHash('sha256').update(raw).digest('hex'), lastFourCharacters: raw.slice(-4), scopes: Array.isArray(req.body.scopes) ? req.body.scopes : [], createdById: req.user.id, expiresAt: req.body.expiresAt ? new Date(req.body.expiresAt) : null } }); await activity(project.workspaceId, req.user.id, 'API_KEY_CREATED', 'api_key', key.id); page(res, { ...key, key: raw }, 201); }
async function revokeApiKey(req, res) { const key = await prisma.apiKey.findUnique({ where: { id: req.params.apiKeyId }, include: { project: true } }); if (!key) throw fail('API key not found', 404); await requireWorkspaceRole(req.user.id, key.project.workspaceId, ADMIN_ROLES); await prisma.apiKey.delete({ where: { id: key.id } }); res.status(204).end(); }

module.exports = { listWorkspaces, createWorkspace, getWorkspace, updateWorkspace, removeWorkspace, listProjects, createProject, getProject, updateProject, removeProject, listCollections, createCollection, updateCollection, removeCollection, listRequests, createRequest, updateRequest, removeRequest, listEnvironments, createEnvironment, listSecrets, createSecret, removeSecret, listApiKeys, createApiKey, revokeApiKey };
